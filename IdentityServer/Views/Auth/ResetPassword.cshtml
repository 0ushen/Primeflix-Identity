@model IdentityServer.ViewModels.Auth.ResetPasswordViewModel


<div class="main">
    <section class="sign-in">
        <div class="container">
            <div class="signin-content">
                <div class="signin-image">
                    <figure><img src="~/images/logo_transparent.png" alt="sing up image"></figure>
                    <a asp-controller="Auth" asp-action="Login" asp-route-returnUrl="@Model.ReturnUrl" class="signup-image-link">Back to login</a>
                </div>
                <div class="signin-form">
                    <h2 class="form-title">Reset your password</h2>
                    <form asp-controller="Auth" asp-action="ResetPassword" method="POST">
                        <div asp-validation-summary="ModelOnly" class="color-error"></div>
                        <input asp-for="ReturnUrl"/>
                        <input asp-for="UserId"/>
                        <input asp-for="Token"/>
                        <div class="form-group">
                            <label asp-for="Password"><i class="zmdi zmdi-lock-outline"></i></label>
                            <input asp-for="Password" id="name" placeholder="Your new password"/>
                            <span asp-validation-for="Password" class="color-error"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for="PasswordConfirmation"><i class="zmdi zmdi-lock-outline"></i></label>
                            <input asp-for="PasswordConfirmation" id="name" placeholder="Your new password confirmation"/>
                            <span asp-validation-for="PasswordConfirmation" class="color-error"></span>
                        </div>
                        <div class="form-group form-button">
                            <input type="submit" name="signin" id="signin" class="form-submit" value="Submit"/>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </section>
</div>
